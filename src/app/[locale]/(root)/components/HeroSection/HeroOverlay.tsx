'use client';

const HeroOverlay = () => {
  return (
    <div className="pointer-events-none absolute inset-0 backdrop-filter-[brightness(80%)]">
      Hero Overlay
    </div>
  );
};

export default HeroOverlay;
